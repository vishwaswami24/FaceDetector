services:
  - type: web
    name: face-detector-backend
    runtime: docker
    plan: free
    region: oregon
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && gunicorn -b 0.0.0.0:${PORT:-5000} app:app

