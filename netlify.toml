[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Vite SPA fallback (safe for this app; no routes besides /)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

